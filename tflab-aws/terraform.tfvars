participant_name = "nikhil"
aws_region       = "us-east-1"

# public_key — paste your SSH public key here, e.g.:
# public_key = "ssh-rsa AAAAB3NzaC1yc2EAAA... labadmin@workstation"
# Generate with: ssh-keygen -t rsa -b 4096 -f ~/.ssh/ailab_key

# Sensitive variables — set via environment variables instead of this file:
#   export TF_VAR_admin_password="YourWindowsPassword"
#   export TF_VAR_db_password="YourDbPassword"
